# API configuration
OPENAI_API_KEY="" 				# Provide a value for OPENAI_API_KEY

# Database configuration - Replace with your actual credentials
DB_HOST="localhost" 				# Provide a value for DB_HOST
DB_PORT=5432 				# Provide a value for DB_PORT
DB_USER="postgres" 				# Provide a value for DB_USER
DB_PASSWORD="postgres" 				# Provide a value for DB_PASSWORD
DB_NAME="chatbot" 				# Provide a value for DB_NAME
DB_ECHO=False 				# Provide a value for DB_ECHO

# DuckDB configuration
DUCKDB_PATH=":memory:" 				# Provide a value for DUCKDB_PATH

# Storage configuration
DATA_DIR="./data" 				# Provide a value for DATA_DIR
LOGS_DIR="./logs" 				# Provide a value for LOGS_DIR
STORAGE_PATH="./data/uploads" 				# Provide a value for STORAGE_PATH
VECTOR_STORE_DIR="./data/vector_store" 				# Provide a value for VECTOR_STORE_DIR

# Authentication
SECRET_KEY="replace-with-secure-secret-key" 				# Provide a value for SECRET_KEY
ACCESS_TOKEN_EXPIRE_MINUTES=1440 				# Provide a value for ACCESS_TOKEN_EXPIRE_MINUTES

# CORS settings
CORS_ORIGINS=["http://localhost:8503"] 				# Provide a value for CORS_ORIGINS

# API configuration
API_V1_STR="/api/v1" 				# Provide a value for API_V1_STR

# PandasAI configuration
MEMORY_SIZE=10 				# Provide a value for MEMORY_SIZE
ENFORCE_PRIVACY=True 				# Provide a value for ENFORCE_PRIVACY
ENABLE_CACHE=True 				# Provide a value for ENABLE_CACHE
MAX_RETRIES=3 				# Provide a value for MAX_RETRIES

# Logging
LOG_LEVEL="INFO" 				# Provide a value for LOG_LEVEL
