[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests that require external services
    streamlit: marks tests that interact with streamlit

addopts = -v --no-header
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_* 